apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: kustomize-helmvalues
  namespace: openshift-gitops
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  destination:
    name: in-cluster
    namespace: openshift-gitops
  project: default
  sources:
    - repoURL: https://github.com/varshab1210/image-updater-examples.git
      targetRevision: pull-secret
      path: kustomization-write-target/source
    - repoURL: https://charts.bitnami.com/bitnami
      targetRevision: "18.2.3"
      chart: nginx
      helm:
        valueFiles:
          - $values/kustomize-helmvalues/source2/values-0.yaml
          - $values/kustomize-helmvalues/source2/values.yaml
    - repoURL: https://github.com/varshab1210/image-updater-examples.git
      targetRevision: pull-secret
      ref: values
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
